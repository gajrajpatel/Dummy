/*#include <stdio.h>

int main() {
    int choice;

    while (1) {
        printf("\n--- Traffic Light System ---\n");
        printf("1. Red Light\n");
        printf("2. Yellow Light\n");
        printf("3. Green Light\n");
        printf("4. Exit\n");
        printf("Enter your choice: ");
        scanf("%d", &choice);

        switch (choice) {
            case 1:
                printf("\nRED LIGHT ON  - STOP!\n");
                sleep(3);  // delay for 3 seconds
                break;
            case 2:
                printf("\nYELLOW LIGHT ON  - READY!\n");
                sleep(2);  // delay for 2 seconds
                break;
            case 3:
                printf("\nGREEN LIGHT ON  - GO!\n");
                sleep(3);  // delay for 3 seconds
                break;
            case 4:
                printf("\nExiting Traffic Light System...\n");
                exit(0);
            default:
                printf("\nInvalid choice! krapya dubara try karein .\n");
        }
    }
    return 0;
}*/
#include<stdio.h>
int main (){
    float inr
}